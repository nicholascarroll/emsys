.Dd July 31, 2026
.Dt EMIL 1
.Os
.Sh NAME
.Nm emil
.Nd text editor
.Sh SYNOPSIS
.Nm
.Op Ar options
.Op Ar file ...
.Sh DESCRIPTION
.Nm
is a small, portable text editor for UTF-8 encoded files.
It is designed to operate on VT100-compatible terminals and implements
a subset of the operational conventions found in the
.Em Emacs
editor.
.Sh OPTIONS
.Bl -tag -width Ds
.It Cm + Ns Ar N
Position the cursor at line
.Ar N
of the first file.
.It Cm --version
Display version information and exit.
.El
.Sh CONCEPTS
.Nm
follows Emacs paradigms.
.Ss Buffers and Windows
A
.Sy buffer
is the in-memory content of a file or text that has not been saved to any file.
A
.Sy window
is a viewport onto a buffer.
Multiple windows can display the same or different buffers simultaneously.
.Ss The Mark Ring
The
.Sy mark
defines one end of a selected region (the other end is the cursor).
.Nm
maintains a ring of the 8 most recent mark positions. Setting the mark pushes the current position onto the ring.  These positions can then be returned to later.
.Ss The Kill Ring
Deleted or copied text is stored in the
.Sy kill ring
(clipboard history).
Yanking retrieves text from the kill ring and inserts it in the buffer ('paste'). Successive yanks can cycle through previous kills, in either direction.
.Ss Universal Argument
The
.Cm C-u
command precedes a numeric argument.
By default,
.Cm C-u
sets the argument to 4.
Repeating
.Cm C-u
multiplies the argument by 4 (e.g.,
.Cm C-u C-u
= 16).
Typing digits after
.Cm C-u
sets the argument to that number.
Negative arguments are not permitted.
.Pp
An alternative key that is equivalent to
.Cm C-u N
(where N is a number) is
.Cm M-N.
.Pp
The
.Cm M--
is the
.Sy reverse modifier
prefix.  Commands that accept this prefix operate in reverse direction when it is provided.  Other commands ignore it.
.Sh COMMANDS
Commands largely follow Emacs conventions.
.Cm C-
denotes the Control key,
.Cm M-
denotes the Meta (Alt) key.
.Cm S-
denotes the Shift key.
.Ss Basic Commands
.Bl -tag -width Ds
.It Cm C-x C-c
Quit the editor.
.It Cm C-x C-f
Open a file and load it into a buffer.
.It Cm C-x C-s
Save the current buffer to its associated file.
With
.Cm C-u ,
saves without creating an ephemeral backup file (risks data loss).
.It Cm C-x C-w
Save the current buffer under a new filename (Save As).
.It Cm C-x C-q
Toggle read-only status of the current buffer.
.It Cm C-x k
Kill (close) the current buffer.
.It Cm C-x b
Switch current window to another buffer.
.It Cm C-z
Suspend the editor; resume with
.Cm fg .
.It Cm C-x C-z
Suspend the editor while preserving the editor screen (Shell Drawer).
.It Cm C-s
Interactive search forward in the current buffer.
.It Cm C-r
Interactive search backward in the current buffer.
.It Cm C-g
Cancel the current operation or deactivate region.
.El
.Ss Cursor Movement
.Bl -tag -width Ds
.It Cm C-n
Move cursor to next line.
.It Cm C-p
Move cursor to previous line.
.It Cm C-f
Move cursor forward character.
.It Cm C-b
Move cursor backward character.
.It Cm M-f
Move cursor forward word.
.It Cm M-b
Move cursor backward word.
.It Cm C-M-f
Move cursor forward s-expression (a word or parenthesised string). 
.It Cm C-M-b
Move cursor backward s-expression.
.It Cm M-a
Move cursor backward sentence.
.It Cm M-e
Move cursor forward sentence.
.It Cm M-{
Move cursor backward paragraph.
.It Cm M-}
Move cursor forward paragraph.
.It Cm M-<
Move cursor to start of buffer.
.It Cm M->
Move cursor to end of buffer.
.It Cm C-a
Move cursor to beginning of line.
.It Cm C-e
Move cursor to end of line.
.It Cm M-v
Page up.
.It Cm C-v
Page down.
.It Cm M-p
Scroll up one line.
.It Cm M-n
Scroll down one line.
.It Cm C-l
Centre the screen on the cursor's current line.
.It Cm M-g
Go to specific line number.
.It Cm C-x =
Displays useful information about the character under the cursor:
character (the visual glyph, or for nonprinting characters, the Unicode description);
Unicode codepoint in hexadecimal; display width; byte offset from the start of the line; absolute byte offset (from the start of the buffer); raw hexadecimal bytes.
.El
.Ss Editing
.Bl -tag -width Ds
.It Cm C-_
Undo.
.It Cm C-x C-_
Redo.
.It Cm C-w
Kill (cut) region or rectangle.
.It Cm M-w
Copy region or rectangle to kill ring.
.It Cm C-c
Copy region to system clipboard (OSC 52) and kill ring.
.It Cm C-y
Yank (paste) from kill ring.
With
.Cm C-u ,
yank leaving the cursor before the inserted text and the mark after it.
.It Cm M-y
Yank pop: Cycle through previous kills after yanking.
With
.Cm M-- ,
cycle in the opposite direction (toward newer kills).
.It Cm C-k
Kill to end of line.
.It Cm M-d
Kill word forward.
.It Cm M-DEL
Kill word backward.
.It Cm M-z
Zap to char: Kill up to and including a given character.
.It Cm C-t
Transpose characters.
With
.Cm M-- ,
drag the character before the cursor backward instead.
.It Cm M-t
Transpose words.
With
.Cm M-- ,
drag the word before the cursor backward instead.
.It Cm C-x C-t
Transpose sentences.
With
.Cm M-- ,
drag the sentence before the cursor backward instead.
.It Cm C-u
Universal Prefix: Followed by a number it repeats the succeeding command.
.It Cm M--
Reverse modifier: Makes the next yank pop, transpose, or word case command act in the opposite direction. Ignored by other commands.
.It Cm C-j
Insert newline and indent (match previous line).
.It Cm C-o
Insert newline without moving the cursor.
.It Cm C-q
Insert next character verbatim (quoted insert).
.It Cm C-x C-l
Convert region to lowercase.
.It Cm C-x C-u
Convert region to uppercase.
.It Cm M-c
Capitalize word.
With
.Cm M-- ,
capitalize the word before the cursor without moving.
.It Cm M-u
Upcase word.
With
.Cm M-- ,
upcase the word before the cursor without moving.
.It Cm M-l
Downcase word.
With
.Cm M-- ,
downcase the word before the cursor without moving.
.It Cm DEL
Delete character under cursor.
.It Cm C-x 8
Insert a character by entering the hex of the Unicode codepoint (insert-char).
.It Cm M-/
Open a palette of emojis and other useful Unicode symbol characters in a popup buffer.
Navigate to the desired character and press
.Cm Return
to insert it at the cursor position.
.El
.Ss Selection and Regions
A
.Sy region
is defined as the text between the
.Sy mark
and the
.Sy cursor .
Once a mark is set, moving the cursor changes the extent of the
active region.
.Bl -tag -width Ds
.It Cm C-SPC
Set mark: Establish the mark at the cursor.
.It Cm C-u C-SPC
Pop mark: Jump to previous mark position (Mark Ring).
.It Cm C-x C-x
Swap cursor and mark positions.
.It Cm C-g
Abort: Deactivate the current region and clear the mark.
.It Cm C-x h
Mark whole buffer: Set the cursor at the beginning and the mark
at the end of the buffer.
.It Cm M-h
Mark the current paragraph.
.El
.Ss Window Management
The display can be partitioned into multiple tiled windows.
.Bl -tag -width Ds
.It Cm C-x o
Other window: Cycle focus to the next visible window.
.It Cm C-x 0
Delete window: Close the currently focused window.
.It Cm C-x 1
Delete other windows: Expand the current window to fill the screen.
.It Cm C-x 2
Split window: Divide the current window horizontally.
.It Cm C-x x t
Toggle word wrap (visual-line-mode).
.El
.Ss Registers
Registers provide session-persistent storage of cursor position and text fragments. Each register is identified by a single character.
.Bl -tag -width Ds
.It Cm C-x r SPACE x
Store the current cursor position in a register named
.Ar x .
.It Cm C-x r j x
Jump to the cursor position stored in register
.Ar x .
.It Cm C-x r s x
Store the active region in register
.Ar x .
.It Cm C-x r y x
Yank contents of register
.Ar x .
.It Cm C-x r k x
Kill region into register
.Ar x .
.It Cm C-x r v x
View contents of register
.Ar x .
.El
.Ss Rectangles
Rectangles operate on column-aligned regions spanning multiple lines.
.Bl -tag -width Ds
.It Cm C-x SPACE
Toggle rectangle mode.
.It Cm C-x r t or C-t
Replace rectangle with string.
.It Cm C-x r k or C-w
Kill rectangle.
.It Cm C-x r y or C-y
Yank rectangle.
.It Cm C-x r o
Open rectangle (insert whitespace).
.El
.Ss Macros
Keyboard macros record a sequence of keystrokes for later playback.
Macros cannot be viewed, edited, exported nor saved.
The commands Find File, Save File and shell commands cannot be executed by macros.
.Bl -tag -width Ds
.It Cm C-x (
Start recording macro.
.It Cm C-x )
Stop recording macro.
.It Cm C-x e
Execute last macro.
.It Cm C-u C-x e
Execute last macro N times.
.El
.Ss Search and Replace
.Bl -tag -width Ds
.It Cm C-M-s
Incremental regular expression search forward.
.It Cm C-M-r
Incremental regular expression search backward.
.It Cm M-%
Query replace (string).
.It Cm M-x query-replace
Replace string interactively.
.It Cm M-x replace-string
Replace all occurrences (non-interactive).
.It Cm M-x replace-regexp
Replace using regular expressions.  Acts on the region while the region is active, and otherwise from
point to the end of the buffer.
.Ql ^
and
.Ql $
anchor at line boundaries.  A newline is entered into either prompt with
.Ic C-q C-j 
.Pp
Within the replacement text:
.Bl -tag -width Ds
.It Ql \&\e&
The text matched by the whole pattern.
.It Ql \&\eN
The text matched by the Nth parenthesised group, for N from 1 to 9.
.It Ql \&\en
Not a newline escape; it is rejected as an invalid backslash sequence.
.It Ql \&\e\e
A literal backslash.
.El
.Pp
An invalid pattern is reported and nothing is replaced.
.El
.Ss Regular Expressions
Patterns are POSIX extended regular expressions, not Emacs or GNU
syntax.
Grouping and alternation are written
.Ql ()
and
.Ql \&| ,
unescaped.
The GNU and Emacs shorthands
.Ql \ew ,
.Ql \eb ,
.Ql \e< ,
non-greedy operators such as
.Ql *? ,
and backreferences within the pattern itself are not available.
.Ss Shell Integration
When enabled at compile time:
.Bl -tag -width Ds
.It Cm M-|
Filter the selected region through a shell command.
A running command can be interrupted with C-g (SIGINT); a second
C-g force-kills it.  The buffer is left unmodified.
.It Cm C-u M-|
Filter the selected region and replace with output.
.It Cm M-!
Run a shell command (output to buffer).
.It Cm M-x diff-buffer-with-file
Show unsaved changes compared to file on disk.
.It Cm M-x cd
Change working directory (editor only).
.El
.Ss Source File Navigation
.Bl -tag -width Ds
.It Cm M-.
Jump to symbol definition (Ctags).
The symbol is the identifier at point or, in Thai text, the word at
point, delimited by U+200B ZERO WIDTH SPACE or by any character that
is not part of a Thai word.
.It Cm M-,
Jump back from symbol definition.
.It Cm M-`
Toggle header and body
.El
.Ss Named Commands
The following commands are available via
.Cm M-x :
.Bl -tag -width Ds
.It Cm capitalize-region
Capitalize each word in the region.
.It Cm insert-char
Insert a character by entering the hex of the Unicode codepoint.
.It Cm insert-file
Insert contents of another file at cursor.
.It Cm cd
Change working directory.
.It Cm diff-buffer-with-file
Show unsaved changes.
.It Cm isearch-forward-regexp
Incremental regex search forward.
.It Cm query-replace
Interactive search-and-replace.
.It Cm replace-regexp
Replace using regular expressions.
.It Cm replace-string
Replace literal strings.
.It Cm revert-buffer
Revert buffer from file on disk.
.It Cm visual-line-mode
Toggle line wrapping.
.It Cm version
Show version info.
.It Cm view-register
Display contents of a register.
.El
.Sh DEVIATIONS FROM EMACS
.Nm
aims for Emacs compatibility but has some differences in functionality: it uses POSIX regex and the following keybindings differ:
.Bl -tag -width Ds
.It Cm C-c
In Emacs, this is the user defined keybinding prefix. In
.Nm ,
it copies to the system clipboard (OSC 52) as well as the kill ring.
.It Cm C-x C-z
In Emacs, this suspends and clears the screen. In
.Nm ,
it suspends while preserving the editor screen (Shell Drawer).
.It Cm C-_
In Emacs, this is both undo and redo, creating an undo history tree by reversal of traversal direction. In
.Nm ,
it is only undo (undo/redo is linear).
.El
.Sh FILES AND ENCODING
.Nm
expects all input and files to be encoded as
.Sy UTF-8 .
Files are loaded in their entirety into memory.
.Pp
.Nm
adheres to the Unix convention where lines are delimited by a single linefeed (LF) character and files terminate with a linefeed.
A file opened without a terminating linefeed gains one when it is loaded.
Validation at file load and inserting text in the buffer disallow  null bytes (0x00) or malformed UTF-8 sequences.
.Ss File Persistence
When saving,
.Nm
writes the complete buffer to the associated file. Symbolic links are followed.  The inode is preserved; hard links are preserved. Before writing, 
.Nm
copies the file to an ephemeral backup (suffixed "~") in the same directory.  The file is then truncated to zero bytes before being written. The write is not atomic; a failed write leaves the file incomplete and 
.Nm
prompts the user with the error message and the backup filename.
.Ss Advisory Locking
While having unsaved edits to a file,
.Nm
will keep an advisory lock on the file. If another instance of
.Nm
begins to edit the file, it receives a warning. The lock does not prevent other processes from writing to the file.
.Ss File Size Limit
.Nm
is not designed for editing very large files. Files larger than 1 GiB cannot be opened.
.Sh EXIT STATUS
.Nm
exits 0 on success and 1 if a file cannot be opened or an unrecognised
option is given.
.Sh SEE ALSO
.Xr emacs 1
.Sh AUTHORS
The
.Nm
editor was developed by
.An Nicholas Carroll .
It is a derivative of
.Em emsys
by
.An japanoise .
.Lk https://github.com/japanoise/emsys
.Sh BUGS
Please report defects to the project repository at:
.Lk https://github.com/nicholascarroll/emil
